// types.ts
export type RootState = {
  slider: SliderState;
  // Add other slices as needed
};

export type SliderState = {
  isToggle: boolean;
  // Add other state properties specific to the slider slice
};

// Define other types as needed
